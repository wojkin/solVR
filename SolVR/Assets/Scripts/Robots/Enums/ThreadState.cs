namespace Robots.Enums
{
    /// <summary>
    /// An enum representing the state in which a robots' thread is.
    /// </summary>
    public enum ThreadState
    {
        Idle,
        Executing,
        Blocked
    }
}