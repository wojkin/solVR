namespace Tasks
{
    /// <summary>
    /// Enum that represents a task state of completion. 
    /// </summary>
    public enum TaskCompletionState
    {
        NotCompleted,
        Completed,
        Failed,
    }
}